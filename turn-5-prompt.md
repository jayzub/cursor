you said the artist-folder leftover "can't be told apart from a real retry." I don't buy that.

last week's merge line for AC; DC is `skip /music/AC; DC`. that folder already has Back In Black, High Voltage and Dirty Deeds in the library. a retry of one skip is one album folder. walking the artist walks the other two. I am not reimporting High Voltage because a merge-then-skip logged the parent.

if a logged path already contains more than one album that is in the library, drop it the same way you drop the library root. a real skip of a single album still comes through.

I also can't follow the logfile path anymore. write-side is skip-first plus the merged-path filter plus `upgrade_declined`. read-side is the semicolon glue plus the windows starts plus the library-root drop. put the read-side filters in one place. don't change what a good skip line does.

and tell me the case where an artist folder *is* the thing I skipped, if there is one. if there isn't, don't leave that limitation in the note.

race and the None text still off. import suite only. no convert, no plugins.
